#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int m, t;
    cin >> m >> t;

    if (m - t <= 0) {
        cout << 0;
    } else {
        cout << m - t;
        /* code */
    }

    return 0;
}